<%= form_with(model: user_reward, local: true) do |form| %>
  <% if user_reward.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_reward.errors.count, "error") %> prohibited this user_reward from being saved:</h2>

      <ul>
      <% user_reward.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_reward_user_id %>
  </div>

  <div class="field">
    <%= form.label :reward_id %>
    <%= form.text_field :reward_id, id: :user_reward_reward_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
