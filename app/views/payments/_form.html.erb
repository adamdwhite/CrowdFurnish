<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :payment_type %>
    <%= form.text_field :payment_type, id: :payment_payment_type %>
  </div>

  <div class="field">
    <%= form.label :account_number %>
    <%= form.number_field :account_number, id: :payment_account_number %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :payment_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
