<%= form_with(model: project_category, local: true) do |form| %>
  <% if project_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_category.errors.count, "error") %> prohibited this project_category from being saved:</h2>

      <ul>
      <% project_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :project_category_category_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :project_category_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
